{
  "name": "foster-douglas",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt generate && echo foster-douglas.com > ./CNAME && node deploy.ts"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "gh-pages": "^6.3.0",
    "nuxt": "^3.15.4"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "eslint": "^9.20.1",
    "nuxi": "^3.22.2",
    "sass": "^1.85.0"
  }
}
